# Install as /etc/elova/frontend.env, owned by root:elova-frontend with mode 0640.
# Use the private Tailnet origin of the GX10 backend; never use a public or
# loopback origin in production. Do not add NEXT_PUBLIC_ aliases.
ELOVA_BACKEND_URL=
