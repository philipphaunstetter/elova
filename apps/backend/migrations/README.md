# Backend migrations

Add ordered, forward-only SQL migrations here when a later authorized slice introduces durable backend state. The separation foundation intentionally has no product-schema migration.
